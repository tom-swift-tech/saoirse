node_modules/
dist/
*.engram
*.engram-shm
*.engram-wal
.env
.env.local
*.log

# Accreted tool-builder artifacts — sandboxed, un-promoted, runtime-only.
sandbox/

# Tier-0 Engram candidate clones — evaluated, un-promoted, runtime-only.
engram-eval/

# Tier-0 authored-change clones — pi-edited local branches, un-published, runtime-only.
engram-author/

# Proactivity scheduler's persisted jobs — runtime state.
jobs.json
